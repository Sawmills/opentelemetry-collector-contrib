# Use this changelog template to create an entry for release notes.

change_type: "enhancement"
component: exporter/loadbalancing
note: Upgrade the Sawmills contrib fork baseline to upstream v0.148.0 and retain Sawmills-specific queue payload compression and post-routing log batching support in the loadbalancing exporter.
issues: [6831]
subtext: |
  Rollout focus:
  - validate pipelines that rely on the Sawmills-only loadbalancing log batcher path
  - validate sending_queue payload compression with both snappy and zstd
  - watch for upstream v0.141.0-v0.148.0 component-set changes such as removed or renamed contrib components when rebuilding downstream distributions
change_logs: [user]
